import { UserHistoryPipe } from './user-history.pipe';

describe('UserHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new UserHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
